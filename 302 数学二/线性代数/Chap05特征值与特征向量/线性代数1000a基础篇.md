# 线性代数1000a基础篇 - 第5章 特征值与特征向量

**题目[1]**
已知 $A$ 为 $3$ 阶方阵，$1, 1, 2$ 是 $A$ 的 $3$ 个特征值，$\alpha_1, \alpha_2, \alpha_3$ 为这 $3$ 个特征值对应的特征向量，则 $(\qquad)$.

[A] $\alpha_1, \alpha_2, \alpha_3$ 必为矩阵 $2E - A$ 的特征向量

[B] $\alpha_1 - \alpha_2$ 必为矩阵 $2E - A$ 的特征向量

[C] $\alpha_1 + \alpha_3$ 必为矩阵 $2E - A$ 的特征向量

[D] $\alpha_1, \alpha_2$ 不是矩阵 $2E - A$ 的特征向量，$\alpha_3$ 必为矩阵 $2E - A$ 的特征向量

---

**题目[2]**
设 $A = \begin{bmatrix} 3 & -4 & 0 \\ 4 & -5 & 0 \\ a & 2 & -1 \end{bmatrix}$，若 $A$ 的三重特征值 $\lambda$ 对应两个线性无关的特征向量，则 $a = (\qquad)$.

[A] 1

[B] 2

[C] -1

[D] -2

---

**题目[3]**
已知 $P^{-1}AP = \begin{bmatrix} 1 & 0 & 0 \\ 0 & 3 & 0 \\ 0 & 0 & 3 \end{bmatrix}$，$\alpha_1$ 是矩阵 $A$ 属于特征值 $\lambda = 1$ 的特征向量，$\alpha_2, \alpha_3$ 是矩阵 $A$ 属于特征值 $\lambda = 3$ 的线性无关的特征向量，则矩阵 $P$ 不可以是 $(\qquad)$.

[A] $[\alpha_1, -2\alpha_2, \alpha_3]$

[B] $[\alpha_1, \alpha_2 + \alpha_3, \alpha_2 - 2\alpha_3]$

[C] $[\alpha_1, \alpha_3, \alpha_2]$

[D] $[\alpha_1 + \alpha_2, \alpha_1 - \alpha_2, \alpha_3]$

---

**题目[4]**
$\lambda = -1$ 是 $A$ 的特征值的充分必要条件为 $(\qquad)$.

[A] $A^2 = E$

[B] $r(A + E) < n$

[C] $A$ 中每行元素之和为 $-1$

[D] $A = -E$

---

**题目[5]**
设 $A, P$ 都是 $n$ 阶可逆矩阵，$\lambda, \xi$ 分别是 $A$ 的特征值和对应的特征向量，则 $P^{-1}A^* P$ 的特征值和对应的特征向量分别是 $(\qquad)$.

[A] $\frac{|A|}{\lambda}, P^{-1}\xi$

[B] $\frac{|A|}{\lambda}, \xi$

[C] $\frac{1}{\lambda}, P\xi$

[D] $\frac{1}{\lambda}, P^{-1}\xi$

---

**题目[6]**
设 $A$ 是 $3$ 阶矩阵，将 $A$ 的第 2 列加到第 3 列得矩阵 $B$，再将 $B$ 的第 3 行的 $-1$ 倍加到第 2 行得 $\begin{bmatrix} 1 & 1 & 0 \\ 0 & 2 & 0 \\ 0 & 2 & a \end{bmatrix}$，其中 $a$ 为常数，则 $A$ 的特征值为 $(\qquad)$.

[A] $1, 2, a$

[B] $1, 2, -2$

[C] $1, -1, 2$

[D] $1, a, -a$

---

**题目[7]**
下列矩阵中，不能相似对角化的是 $(\qquad)$.

[A] $\begin{bmatrix} 1 & 2 & -1 \\ 2 & 0 & 1 \\ -1 & 1 & 0 \end{bmatrix}$

[B] $\begin{bmatrix} 3 & 2 & 1 \\ 0 & 2 & 1 \\ 0 & 0 & 0 \end{bmatrix}$

[C] $\begin{bmatrix} 2 & 0 & 0 \\ 0 & 2 & 0 \\ 1 & 0 & 1 \end{bmatrix}$

[D] $\begin{bmatrix} 0 & 0 & 0 \\ 1 & 2 & 0 \\ 0 & 1 & 2 \end{bmatrix}$

---

**题目[8]**
设 $A$ 为 $2$ 阶矩阵，$\alpha = \begin{bmatrix} 1 \\ 0 \end{bmatrix}$ 是方程组 $A\boldsymbol{x} = \boldsymbol{0}$ 的解，$\beta = \begin{bmatrix} 1 \\ 1 \end{bmatrix}$ 是方程组 $A\boldsymbol{x} = \beta$ 的解，则矩阵 $A = \underline{\qquad\qquad}$.

---

**题目[9]**
已知 $A$ 为 $2$ 阶方阵，可逆矩阵 $P = [\alpha, \beta]$ 使得 $P^{-1}AP = \begin{bmatrix} 1 & 0 \\ 0 & 2 \end{bmatrix}$，$Q = [\beta, \alpha]$，则 $Q^{-1}A^* Q = \underline{\qquad\qquad}$.

---

**题目[10]**
设 $1$ 与 $-1$ 是矩阵 $A = \begin{bmatrix} 3 & 1 & -2 \\ -a & -1 & a \\ 4 & 1 & -3 \end{bmatrix}$ 的特征值，若矩阵 $A$ 可相似对角化，则 $a = (\qquad)$.

[A] -1

[B] 0

[C] 1

[D] 2

---

**题目[11]**
已知 $A = \begin{bmatrix} 1 & -1 & -a \\ 2 & a & -2 \\ -a & -1 & 1 \end{bmatrix}$，求 $A$ 的特征值，并讨论 $A$ 是否可相似对角化.

---

**题目[12]**
设 $A$ 是 $3$ 阶实对称矩阵，已知 $A$ 的每行元素之和为 $3$，且有二重特征值 $\lambda_1 = \lambda_2 = 1$. 求 $A$ 的全部特征值、特征向量，并求 $A^n$.

---

**题目[13]**
设 $A$ 是 $3$ 阶实对称矩阵，满足 $A + A^2 + \frac{1}{2}A^3 = O$，则 $r(A) = \underline{\qquad\qquad}$.

---

**题目[14]**
(1) 设 $A, B$ 是 $n$ 阶矩阵，$A$ 有特征值 $\lambda = 1, 2, \cdots, n$. 证明 $AB$ 和 $BA$ 有相同的特征值且 $AB \sim BA$；

(2) 对一般的 $n$ 阶矩阵 $A, B$，是否必有 $AB \sim BA$，说明理由.

---

**题目[15]**
已知 $A = \begin{bmatrix} 3 & a & 4 \\ 2 & -1 & 2 \\ -2 & -a & -3 \end{bmatrix}$，求 $A$ 的特征值，问 $a$ 为何值时，$A$ 不能相似于对角矩阵；$a$ 为何值时，$A$ 相似于对角矩阵，并求可逆矩阵 $P$，使得 $P^{-1}AP = \Lambda$.

---

**题目[16]**
已知 $3$ 阶矩阵 $A$ 有三个特征值 $-1, 2, \frac{1}{3}$，对应的特征向量分别是 $\xi_1, \xi_2, \xi_3$，取 $P = [2\xi_2, -\xi_1, 3\xi_3]$，则 $P^{-1}AP = \underline{\qquad\qquad}$.

---

**题目[17]**
已知 $A$ 是 $3$ 阶矩阵，$r(A) = 1$，则 $\lambda = 0$ 是 $A$ 的特征值，其重数 $(\qquad)$.

[A] 必为 2

[B] 可能为 2 或 3

[C] 可能为 1 或 2

[D] 可能为 1, 2 或 3

---

**题目[18]**
设 $A$ 是 $2$ 阶矩阵，有特征值 $\lambda_1 = 1, \lambda_2 = 2$，$f(x) = x^2 - 3x + 3$，则 $f(A) = \underline{\qquad\qquad}$.

---

**题目[19]**
设 $A, B$ 是 $n$ 阶可逆矩阵，且 $A \sim B$，则以下结论中：
① $A^{-1} \sim B^{-1}$；
② $A^\mathrm{T} \sim B^\mathrm{T}$；
③ $A^* \sim B^*$；
④ $AB \sim BA$.
正确结论的个数是 $(\qquad)$.

[A] 1

[B] 2

[C] 3

[D] 4

---

**题目[20]**
设 $A = E - \frac{3}{\alpha^\mathrm{T}\alpha}\alpha\alpha^\mathrm{T}$，其中 $E$ 是 $n$ 阶单位矩阵，$\alpha = [a_1, a_2, \cdots, a_n]^\mathrm{T} \ne \boldsymbol{0}$.

(1) 计算 $A^2$，并求 $A^{-1}$；

(2) 验证 $\alpha$ 是 $A$ 的特征向量，并求 $A$ 的对应于 $\alpha$ 的特征值.

---

**题目[21]**
设 $A$ 是 $3$ 阶矩阵，$\lambda_0$ 是 $A$ 的特征值，对应的特征向量为 $\xi = [1, 1, 1]^\mathrm{T}$，且 $|A| = 1$，$A^*$ 是 $A$ 的伴随矩阵，$A^* = \begin{bmatrix} -a & 1 & -2 \\ -1 & b & -\frac{7}{2} \\ 2 & -3 & a \end{bmatrix}$，求参数 $a, b$ 及 $\lambda_0$.

---

**题目[22]**
设 $A$ 是 $3$ 阶矩阵，$\lambda_1, \lambda_2, \lambda_3$ 是 $A$ 的三个不同的特征值，对应的特征向量分别是 $\xi_1, \xi_2, \xi_3$，令 $\beta = \xi_1 + \xi_2 + \xi_3$. 证明：

(1) $\beta$ 不是 $A$ 的特征向量；

(2) 向量组 $\beta, A\beta, A^2\beta$ 线性无关.

---

**题目[23]**
设 $A$ 为 $3$ 阶实对称矩阵，$\mathrm{tr}(A) = 1$，齐次线性方程组 $A\boldsymbol{x} = \boldsymbol{0}$ 的通解为 $\boldsymbol{x} = k_1[-2, 1, 0]^\mathrm{T} + k_2[-3, 0, 1]^\mathrm{T}$，$k_1, k_2$ 为任意常数，求 $A^n$.
